<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shenhe Bot</title>
    <link class="rounded" rel="icon" type="image/x-icon" href="https://i.imgur.com/niKICvu.jpg">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Kanit:wght@300&display=swap');

        body {
            font-family: "Kanit", sans-serif;
            font-weight: 500;
            font-style: normal;
        }
    </style>
</head>
<body>
    <div class="flex items-center justify-center min-h-screen bg-gray-100">
        <div class="text-center">
            <h1 class="text-3xl font-bold">Status</h1>
            <p>Bot Name: <span id="bot-name"><%= info.bot_name %></span></p>
            <p>Online at: <span id="start-time"><%= info.start_time %></span></p>
            <div id="ping-status">
                API ping is <span id="api-ping-value"><%= info.api_ping %></span> ms
            </div>
        </div>
    </div>

    <script>
        function updatePing() {
            fetch('/api/status')
                .then(response => response.json())
                .then(data => {
                    document.getElementById('bot-name').textContent = data.bot_name;
                    document.getElementById('api-ping-value').textContent = data.api_ping;
                });
        }

        // Update ping every 5 seconds
        setInterval(updatePing, 5000);

        // Optionally, update ping immediately on page load
        updatePing();
    </script>
</body>
</html>